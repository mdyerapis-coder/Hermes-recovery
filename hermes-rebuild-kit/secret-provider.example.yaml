secret_provider:
  backend: bitwarden
  vault: default
  folder: "AI & LLM/API Keys/Active"
  cache: session
  validate: true
  fail_closed: true

  items:
    openrouter: "OpenRouter — API Key — Primary"
    opencode: "OpenCode — API Key — Free Tier"
    opencode_go: "OpenCode — API Key — Hermes Go"
